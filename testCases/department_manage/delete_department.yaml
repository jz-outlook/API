-
  name: 删除部门
  request:
    method: delete
    url: /user/department/delete/${read_extract_data(parentId)}
    data:
      id: ${read_extract_data(parentId)}
    headers:
      accessToken: ${read_extract_data(accessToken)}
  validate:
    - equals: {code: 200}
    - equals: {message: '成功'}