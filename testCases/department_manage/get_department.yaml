-
  name: 查询部门
  request:
    method: get
    url: /user/department/get/${read_extract_data(parentId)}
    data:
      id: ${read_extract_data(parentId)}
    headers:
      accessToken: ${read_extract_data(accessToken)}
  validate:
    - equals: {code: 200}
    - equals: {message: '成功'}
