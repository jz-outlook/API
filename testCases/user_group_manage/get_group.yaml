-
  name: 查询用户组
  request:
    method: get
    url: /user/group/get/${read_extract_data(group_id)}
    params:
    headers:
      accessToken: ${read_extract_data(accessToken)}
  validate:
    - equals: {code: 200}
    - equals: {message: '成功'}
