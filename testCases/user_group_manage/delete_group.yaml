-
  name: 删除用户组
  request:
    method: delete
    url: /user/group/delete/${read_extract_data(group_id)}
    params:
    headers:
      accessToken: ${read_extract_data(accessToken)}
  validate:
    - equals: {code: 200}
    - equals: {message: '成功'}
